services:

  azurite:
    image: mcr.microsoft.com/azure-storage/azurite
    container_name: azurite
    command: azurite --blobHost 0.0.0.0
    ports:
      - "10000:10000"
    volumes:
      - ./azurite:/data